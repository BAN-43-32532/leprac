#ifndef TYPE_H
#define TYPE_H

enum class Style { dark, light, classic, custom };
enum class Lang { en, zh, ja };

enum class GameId {
  Le01,
  Le02,
  Le03,
  Le04,
  Uso,  // 束方 嘘時空　～ Fools_rush_in
};

#endif //TYPE_H
